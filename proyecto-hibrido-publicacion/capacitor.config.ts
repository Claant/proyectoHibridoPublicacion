import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proyecto.hibrido.publicacion',
  appName: 'proyecto-hibrido-publicacion',
  webDir: 'www'
};

export default config;
